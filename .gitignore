# Local-only internal design artifacts (moved here during restructure)
_archive/

# Repo-specific Claude Code state (not part of the deliverable — except hooks and settings)
.claude/*
!.claude/settings.json
!.claude/hooks

# Session artifacts — not committed; use docs/ for committed reference material
plans/
tests/
skills/git-manager-workspace/
research/

# Local navigation registry — working artifact, not a committed deliverable
/TODO.md

# Test harness for setup script
scripts/setup-test.sh

# Per-machine symlink created by setup.sh (co-locates the librarian runtime
# bundle into its skill dir). Not committed — a committed symlink materializes
# as a plain text file on fresh Windows clones (core.symlinks=false).
skills/librarian/librarian.workflow.mjs

# Standard ignores
node_modules/
.DS_Store
*.pyc
__pycache__/
.env
credentials*
